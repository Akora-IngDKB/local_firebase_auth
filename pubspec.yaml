name: local_firebase_auth
description: An experimental dart package that mocks (emulates) the firebase_auth plugin for quick and offline development.
version: 0.1.0
homepage: https://github.com/Akora-IngDKB/local_firebase_auth.git

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  hive: ^1.4.4
  meta: ^1.2.3
  random_string: ^2.1.0

dev_dependencies:
  test: ^1.15.4
