name: local_firebase_auth
description: A dart package that mocks (emulates) the firebase_auth plugin for easy and offline development.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0 <2.0.0"

dependencies:
  flutter:
    sdk: flutter
  random_string: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
